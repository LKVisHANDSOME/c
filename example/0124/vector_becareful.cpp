#include <bits/stdc++.h>
using namespace std;

void invalid() {
    int n;
    cin >> n;
    vector<int> v;
    for (int i = 0; i < n; i++) {
        cin >> v[i];
    }
}

void valid() {
    int n;
    cin >> n;
    vector<int> v(n);
    for (int i = 0; i < n; i++) {
        cin >> v[i];
    }
}

int main()
{
    system("pause");
    return 0;
}